name: Run stresser
on: workflow_dispatch
jobs:
  compare:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - run: |
          sudo apt update && sudo apt install miniupnpc -y
          upnpc -s
