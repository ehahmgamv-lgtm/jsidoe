name: Run stresser
on: workflow_dispatch
jobs:
  compare:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - run: |
          gcc flood.c -o flood -std=c2x -pthread -O3 -flto -march=native
